# Redacted generated dependency tree

This directory represented a local virtual environment in the source workspace. It was not copied to the public export because it contains generated dependencies, bytecode, executables, certificates, and local environment metadata. Original entries are accounted for in SANITIZATION_MANIFEST.csv with redacted paths.
